# hy-tool

Companion tooling for [hy](https://github.com/wusir27/hy).

## Linux server install

Script: [`install/install_server.sh`](install/install_server.sh)

Details, path table vs official Hysteria, and `--local` when there is no GitHub Release: [`install/README.md`](install/README.md)

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/wusir27/hy-tool/main/install/install_server.sh)
```
